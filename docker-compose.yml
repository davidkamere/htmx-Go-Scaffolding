version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      PORT: "8080"
      APP_ENV: "production"
      LOG_LEVEL: "info"
      DB_PATH: "/app/data/tasks.json"
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
